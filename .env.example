# Copy this file to `.env`, fill in the secrets you intend to use locally,
# and NEVER commit the populated file.

# =============================================================================
# LLM Provider selection + global retry/backoff
# =============================================================================
# e.g. glm | qwen | perplexity | doubao | moonshot | deepseek | grok | gemini
LLM_PROVIDER=glm
LLM_RETRIES=3
LLM_BACKOFF_BASE=0.5
LLM_REQUEST_TIMEOUT=60

# Task-specific model defaults (used by decomposer/executor if not overridden)
DECOMP_MODEL=qwen3-max
PLAN_EXECUTOR_MODEL=qwen3-max

# =============================================================================
# Provider-specific credentials
# Only fill the ones you actually use. Leave others blank.
# =============================================================================

## Qwen (high quota / high concurrency)
QWEN_API_KEY=
QWEN_API_URL=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
QWEN_MODEL=qwen3-max

## GLM (fallback /-compatible default)
GLM_API_KEY=
GLM_API_URL=https://open.bigmodel.cn/api/paas/v4/chat/completions
GLM_MODEL=glm-4-flash

## Perplexity
PERPLEXITY_API_KEY=
PERPLEXITY_API_URL=https://api.perplexity.ai/chat/completions
PERPLEXITY_MODEL=sonar-reasoning-pro

## Doubao (Volcengine Ark)
DOUBAO_API_KEY=
DOUBAO_API_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=doubao-seed-1-6-251015

## Moonshot / Kimi
MOONSHOT_API_KEY=
MOONSHOT_API_URL=https://api.moonshot.cn/v1
MOONSHOT_MODEL=kimi-k2-turbo-preview

## DeepSeek
DEEPSEEK_API_KEY=
DEEPSEEK_API_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

## Grok / xAI
GROK_API_KEY=
GROK_API_URL=https://api.x.ai/v1
GROK_MODEL=grok-4

## Gemini (OpenAI-compatible endpoint)
GEMINI_API_KEY=
GEMINI_API_URL=https://generativelanguage.googleapis.com/v1beta/openai/
GEMINI_MODEL=gemini-2.5-flash

## Other optional providers already supported elsewhere
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
TAVILY_API_KEY=


# =============================================================================
# Core application configuration
# =============================================================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=9000
BASE_URL=http://0.0.0.0:9000
DATABASE_URL=sqlite:///./tasks.db
LOG_LEVEL=INFO

# Frontend dev server defaults (used for local Vite dev/proxy hints)
VITE_DEV_SERVER_HOST=0.0.0.0
VITE_DEV_SERVER_PORT=3001
# =============================================================================
# Backend service tuning (timeouts, etc.) can be overridden below as needed.
# =============================================================================
# CORS_ORIGINS=http://localhost:3001
# VITE_API_BASE_URL=http://localhost:9000
# VITE_WS_BASE_URL=ws://localhost:9000